"""
第7章 练习题：Pandas 数据分析实战

场景说明：
    你拿到了一份 RNA-seq 基因表达数据，包含 8 个基因在 6 个样本中的表达量（TPM）。
    其中 3 个样本来自癌症组织（cancer），3 个来自正常组织（normal）。
    你需要找出在癌症中显著上调的基因。
"""

import pandas as pd
import numpy as np

# ============================================================
# 示例数据（直接运行即可使用）
# ============================================================

# 基因表达数据
gene_expression = pd.DataFrame({
    '基因名':    ['TP53', 'BRCA1', 'MYC', 'EGFR', 'GAPDH', 'VEGFA', 'CDK4', 'RB1'],
    'cancer_1': [15.2,   3.1,    48.5,  35.0,   100.2,   42.3,   28.7,   4.2],
    'cancer_2': [13.8,   2.8,    52.1,  30.5,   99.1,    38.9,   25.4,   3.8],
    'cancer_3': [16.5,   3.5,    46.7,  33.2,   101.5,   45.1,   30.2,   5.1],
    'normal_1': [6.1,    7.8,    9.5,   5.8,    100.8,   8.2,    10.3,   12.5],
    'normal_2': [5.5,    8.2,    11.2,  6.3,    99.5,    7.5,    11.8,   11.8],
    'normal_3': [6.8,    7.1,    10.8,  5.1,    101.2,   9.1,    9.7,    13.2],
})

# 样本分组信息
sample_info = pd.DataFrame({
    '样本名': ['cancer_1', 'cancer_2', 'cancer_3',
              'normal_1', 'normal_2', 'normal_3'],
    '分组':   ['cancer', 'cancer', 'cancer',
              'normal', 'normal', 'normal'],
})

print("=== 基因表达数据 ===")
print(gene_expression)
print("\n=== 样本分组信息 ===")
print(sample_info)


# ============================================================
# 练习1：找出癌症中上调 2 倍以上的基因
# ============================================================
#
# 任务：找出满足以下条件的基因：
#   cancer 组平均表达量 > normal 组平均表达量的 2 倍
#
# 预期结果：应该能找到 TP53、MYC、EGFR、VEGFA、CDK4 等基因
#
# 提示（按步骤思考）：
#   步骤1：分别拿到 cancer 组和 normal 组的样本列名列表
#          → 可以用 sample_info 筛选，也可以直接手写列名
#   步骤2：计算每个基因在 cancer 组的平均表达量（对列求均值）
#          → 提示：df[列名列表].mean(axis=1) 可以按行求均值
#   步骤3：同理计算 normal 组的平均表达量
#   步骤4：计算 Fold Change = cancer均值 / normal均值
#   步骤5：筛选 Fold Change > 2 的基因
#
# 在下方写你的代码：
# -----------------------------------------------------------

# your code here


# ============================================================
# 练习2：将筛选结果保存为 CSV 文件
# ============================================================
#
# 任务：将练习1中筛选出的上调基因信息保存到 CSV 文件
#   文件名：upregulated_genes.csv
#   要求包含列：基因名、cancer平均表达量、normal平均表达量、Fold Change
#
# 提示：
#   步骤1：先用练习1的结果构建一个新的 DataFrame
#          → pd.DataFrame({'列名1': 数据1, '列名2': 数据2, ...})
#   步骤2：用 df.to_csv() 保存
#          → 记得加 index=False 避免保存多余的行号
#
# 在下方写你的代码：
# -----------------------------------------------------------

# your code here
